{
  "name": "cp-openapi-gen-plugin",
  "version": "0.0.1",
  "description": "Node.js for OpenAPI specification generation using @openapitools/openapi-generator-cli",
  "main": "main.js",
  "type": "module",
  "bin": {
    "cp-openapi-gen": "./main.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-library": "bash -O extglob -c 'rm -rf dist && mkdir dist && cp -r !(dist|node_modules|*.log) dist'"
  },
  "author": "CleverPine",
  "contributes": {
    "name": "Borislav Kibarov"
  },
  "license": "MIT",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.9.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
